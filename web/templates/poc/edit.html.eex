<h2>Edit poc</h2>

<%= render "form.html", changeset: @changeset,
                        action: poc_path(@conn, :update, @poc),
                        companies: @companies %>

<%= link "Back", to: poc_path(@conn, :index) %>
