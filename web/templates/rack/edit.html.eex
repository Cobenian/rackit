<h2>Edit rack</h2>

<%= render "form.html", changeset: @changeset,
                        action: rack_path(@conn, :update, @rack),
                        cages: @cages %>

<%= link "Back", to: rack_path(@conn, :index) %>
