<h2>New room</h2>

<%= render "form.html", changeset: @changeset,
                        action: room_path(@conn, :create),
                        floors: @floors %>

<%= link "Back", to: room_path(@conn, :index) %>
